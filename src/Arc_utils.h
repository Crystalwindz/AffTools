#ifndef ARC_UTILS_H
#define ARC_UTILS_H
#include <string>
#include<QTextEdit>
#include<QLine>
void to_arc_num(double &x,double &y);
void to_mouse_num(double &x,double &y);
int stoi(const std::string& i, size_t b, size_t e);
double stod(const std::string& i, size_t b, size_t e);
#endif // ARC_UTILS_H
